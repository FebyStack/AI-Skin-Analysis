# deps / build
node_modules/
dist/
dist-server/
*.tsbuildinfo
.tsbuild/
vite.config.d.ts
vite.config.js
vitest.config.d.ts
vitest.config.js

# secrets & env — never commit
.env
.env.*
!.env.example
*.pem
*.key
secrets/

# patient data / DB dumps — never commit
backup-*.sql
*.dump
data/
*.sqlite

# ML model assets (provided out-of-band, not versioned — see ai/models/fetch_models.py)
*.onnx
*.onnx.data
*.pth
*.pt
*.safetensors
ai/models/**/best_checkpoint.pth
# Vendored upstream clone (181MB, its own git repo) — install via pip / fetch script instead
MobileSAM/

# tooling scratch
.superpowers/
.worktrees/
.venv/
.DS_Store
__pycache__/
*.task
ai/datasets/
ai/models/**/*.pt

# Kept off iCloud via .nosync suffix; `node_modules`/`.venv` are symlinks into these.
node_modules
node_modules.nosync/
.venv
.venv.nosync/
