# AI Models

Model registry & provenance. Browser-served ONNX artifacts are deployed to `frontend/public/models/` (the browser must fetch them at `/models/*`); this folder documents/manages them. Dummy model generator: `ai/training/generate_dummy_model.py`.
